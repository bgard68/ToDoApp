# Copy to .env for docker compose. Do NOT commit the real .env.
# Generate a key with: openssl rand -base64 48
JWT_KEY=

# Optional: Google OAuth Web client ID (public value). Leave blank to disable Google sign-in.
GOOGLE_CLIENT_ID=
